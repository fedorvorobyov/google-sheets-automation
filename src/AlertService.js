/**
 * AlertService — email notifications on budget exceed.
 * TODO: Implement in Feature #1229.
 */

function sendAlert(subject, body) {
  Logger.log('TODO: AlertService.sendAlert');
}

if (typeof module !== 'undefined') {
  module.exports = { sendAlert };
}
